[tox]
envlist = py39,py310,py311,py312,py313

[testenv]
skip_install = true
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/test-requirements.txt
commands =
    pytest --cov=chickenstats_api
